#ifndef _ASM_IOCTLS_H
#define _ASM_IOCTLS_H

/* This file contains the ioctl numbers defined in the SVr4 kernel. */
/* Right now, these match exactly the numbers used in the original kernel by AT&T, but if I add any in the SVr4 reborn project, I will add them here and gate them by the appopriate compiler defines. */


#endif /* _ASM_IOCTLS_H */
